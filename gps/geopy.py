import geopy.geocoders as gc

# agent = gc.Nominatim(user_agent="Geopy Library")

# address = input()

# location = agent.geocode(address)

# print(location.address)
# print(location.latitude)
# print(location.longitude)
# print(location.raw)